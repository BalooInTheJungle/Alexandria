# Supabase (obligatoire)
# Project URL : https://xxx.supabase.co (Settings → API → Project URL)
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# OpenAI (RAG : génération des réponses)
# Utilisé côté serveur uniquement (API route /api/rag/chat).
OPENAI_API_KEY=your-openai-api-key

# Cron rétention 30 jours (optionnel)
# Protège GET /api/cron/retention. Vercel envoie ce secret en Authorization: Bearer. Pour script manuel : ?secret=xxx
CRON_SECRET=
