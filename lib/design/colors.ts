/**
 * Couleurs de la charte Alexandria.
 * Import : import { colors } from "@/lib/design"
 */

/** Couleur d’action (boutons, liens, accents) */
export const ACTION = "#FDC843";

/** Couleur sombre (texte, éléments forts) */
export const DARK = "#1C1404";

/** Fond blanc (conservé) */
export const BACKGROUND = "#FFFFFF";

/** Export groupé */
export const colors = {
  action: ACTION,
  dark: DARK,
  background: BACKGROUND,
} as const;
